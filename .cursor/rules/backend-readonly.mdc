---
description: 本仓库后端目录只读，助手不得修改 backend/
alwaysApply: true
---

# 后端只读（smartlunchbox）

- **不要**在 `backend/` 下新增、修改或删除任何文件（含 Go 源码、测试、`go.mod`、脚本等），除非用户在同一条消息里**明确**要求改后端。
- 功能缺口（如新 API）请只在前端或其它非后端目录处理：调用已有接口、做 UI 说明、或向用户说明需由后端团队自行实现。
- 若用户说「不要改后端」「后端不要动」等，一律遵守本规则。
